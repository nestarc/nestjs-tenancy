# Documentation remediation checklist — 2026-09-10

This is the fixed implementation checklist for the findings in [the audit](./2026-09-10-documentation-audit.md). IDs and scope are fixed. All 20 implementation items are complete; the evidence and publication boundary are recorded below. It covers this package, its official documentation, and the repository discovery settings. Historical audit observations remain historical.

| ID | Status | Fixed scope | Completion evidence |
| --- | --- | --- | --- |
| D01 | Complete | Correct authentication middleware ordering and prove authentication precedes tenant resolution | [HTTP guide](./http.md#authentication-before-tenant-resolution), real Nest bootstrap in `examples/quickstart/app.ts`; request checks prove authentication precedes extraction. |
| D02 | Complete | Remove the incomplete admin bypass-flag policy and document authorized administration with current restrictive RLS policies | [Prisma administration](./prisma.md) and [operations](./operations.md) remove the flag-only bypass recipe; canonical restrictive policies pass live `doctor --active` and cross-tenant write checks. |
| D03 | Complete | Correct sharedModels/withoutTenant client behavior versus database RLS throughout active documentation and JSDoc | [Prisma guide](./prisma.md), `src/prisma/prisma-tenancy.extension.ts`, site overview/agent guide/API prose distinguish extension handling from database privileges. |
| D04 | Complete | Make Path and Composite identifier examples agree with HTTP validation | [HTTP guide](./http.md) and site extractor examples include slug validators for Path/Composite; actual configuration snippets compile. |
| D05 | Complete | Fix strict TypeScript user/cookie/response examples and separate lifecycle choices | Actual HTTP/API examples narrow unknown properties and use an Express response intersection; 21 package and 13 site snippets pass strict TypeScript. |
| D06 | Complete | State HTTP adapter prerequisites accurately and cover path extraction from raw request URLs | `PathTenantExtractor` falls back to raw `url`, with precedence/malformed/query/hash tests; package/site guides state adapter prerequisites and distinguish this unreleased fix from published 0.16.0. |
| D07 | Complete | Document automatic tenant overwrite, upsert update handling, nested writes, and input-type limits | [Prisma guide](./prisma.md), source option JSDoc, and generated API editorial notes describe overwrite, `upsert.update` removal, top-level scope, and required Prisma input fields. |
| D08 | Complete | Make cache TTL units and intended durations explicit | [Integrations](./integrations.md) and site caching guide use `60_000`/`300_000` with millisecond units. |
| D09 | Complete | Replace one-line, zero-overhead, and blanket SQL-injection claims with supported statements | README, active package/site guides, CLI comments, and extension JSDoc now scope transaction cost and bound SQL values accurately. |
| D10 | Complete | Make benchmark evidence reproducible with provenance and remove unsupported historical headline claims | [Benchmark procedure](../benchmarks/README.md), reset opt-in, provenance writer, 5 reporting tests, and [2,500 raw measurements](../benchmarks/results/2026-09-10-documentation-remediation.json); unsupported historical headline measurements removed. |
| D11 | Complete | Reorder README for first use, add navigation, and put authentication/authorization conditions beside setup | [README](../README.md) is now an entry point with a complete first-run path, nearby authorization conditions, a reference table, and preserved legacy anchors. |
| D12 | Complete | Provide a complete runnable Nest/Prisma example with schema, SQL, seed, auth, module, controller, and commands | [Runnable example](../examples/quickstart/README.md) includes all application/database files and setup/start commands; real Prisma/PostgreSQL verification passed. |
| D13 | Complete | Add meaningful documentation/example validation to CI, including type, links, auth order, and tenant isolation | `scripts/test-docs.js` checks current links/anchors, 21 actual snippets, Nest request behavior, and real database isolation; the new CI documentation job is included in the shared release validation gate. |
| D14 | Complete | Consolidate compatibility/deprecation details while retaining safety notes at their use sites | [Compatibility](./compatibility.md) is the package reference for supported lanes and deprecation timing; detailed guides replace the monolithic README while preserving use-site safety notes. |
| D15 | Complete | Synchronize official tenancy pages, source JSDoc, generated API documentation, and maintenance instructions | 24 official-site files updated; immutable 0.16.0 API signatures/source provenance retained, exact prose corrections marked and reapplied after generation; site and package maintenance instructions added. |
| D16 | Complete | Correct discovery title/description and use a direct tenancy homepage in package metadata | README title/intro identify NestJS, Prisma, PostgreSQL RLS, and multi-tenancy; site description corrected; package homepage points directly to `/packages/tenancy/`. |
| D17 | Complete | Set appropriate GitHub Topics and the repository homepage | GitHub repository settings applied and reread: direct tenancy homepage plus nine relevant Topics (listed below). |
| D18 | Complete | Provide a version-scoped consumer-agent guide and tenancy-specific llms entry points | [Consumer agent guide](./usage-for-agents.md), root `llms.txt`, site agent guide/sidebar, and package-specific site `llms.txt` entries; current guides and example sources included in npm pack. |
| D19 | Complete | Provide a concise maintainer-agent entry point and current documentation index, preserving historical boundaries | Root `AGENTS.md` separates maintenance from consumption; [documentation index](./README.md) identifies current references and history; original audit citations pinned to the inspected commit. |
| D20 | Complete | Preserve and verify existing SEO access settings; document honest limits of ranking/AI-discovery claims | Site output validation passes canonical/OG/structured data/robots/sitemap/link checks; existing access settings preserved, new guide included; no ranking or agent-retrieval guarantee claimed. |

## Verification completed

- `npm run typecheck`, `npm run lint`, and `npm run lint:typed`: passed.
- `npm run test:cov -- --runInBand`: **59 suites / 970 tests passed**. Coverage: statements 98.10%, branches 96.19%, functions 100%, lines 98.73%; required floors passed.
- `npm run test:docs`: links and anchors for 12 current Markdown documents, 21 actual HTTP/API TypeScript snippets, complete example compilation, and real Nest in-process HTTP behavior passed.
- `npm run test:docs:e2e`: isolated PostgreSQL, atomic canonical RLS setup, built `doctor --json --active` healthy, concurrent A/B reads, HTTP create with forged tenant field ignored, unauthorized requests rejected, context-free app-role reads empty, cross-tenant UPDATE affecting zero rows, and cross-tenant INSERT rejected with SQLSTATE 42501. The owned test database was removed. A final extra body-validation assertion and runner cleanup/link refinements passed the non-database command afterward.
- `npm run test:package`: actual npm tarball installed into two fresh consumers with strict peer resolution; root/cache/testing exports, declaration compilation, runtime smoke checks, and CLI passed. The consumer without cache peers also passed.
- Pack inspection: 159 files; intended current docs, agent guide, example sources, and benchmark artifact present; local Markdown file links resolve within the package; generated Prisma clients excluded.
- Benchmark reporting: 5 tests passed. Real benchmark: 5 scenarios × 500 samples after 50 warmups each, unchanged scoped source hashes during the run, isolated PostgreSQL removed afterward. The artifact honestly records a dirty working tree and its measurement-time hashes; later documentation/package-metadata edits are not retroactively attributed to the run.
- Official site `npm run docs:check`: 79 tests and 13-package generated API validation passed. Final build/output validation: 188 public pages / 189 HTML files. The 13 standalone extractor/lifecycle snippets passed strict TypeScript. Immutable-source tenancy API regeneration and idempotent editorial correction passed; original source provenance stayed unchanged.
- All 24 site files applied to the original site repository were byte-compared with the tested scratch files. Both repositories pass `git diff --check`.

## Source and publication state at remediation completion

This section records the state before the subsequent 0.16.1 release work. Release publication and deployment are separate from the completed implementation checklist above.

Package changes are in the local `nestjs-tenancy` worktree, based on commit `91b9fb767a335b15356d4d99069c1619641b8278`. Official-site changes are applied to the separate local `nestarc.dev` worktree. No commit, push, npm publication, or site deployment was performed. Published npm version 0.16.0 and the live website therefore do not yet include these local source changes.

The generated API continues to identify release `v0.16.0` at `68e62e5eab17b24b34287bd91f36fba797d614d3`; `.editorial.json` and a visible note distinguish reviewed prose corrections from released runtime/API behavior. The raw-URL fallback is recorded as an unreleased runtime fix in the package changelog, and published-version site examples include compatibility normalization.

GitHub repository metadata is already applied: homepage `https://nestarc.dev/packages/tenancy/`; Topics `nestjs`, `multi-tenancy`, `multi-tenant`, `prisma`, `postgresql`, `row-level-security`, `tenant-isolation`, `typescript`, and `saas`.

Publish the package repository's new example/benchmark/documentation paths before deploying the site's links to those paths. The package homepage and bundled documentation reach npm on the next release; they do not mutate the existing 0.16.0 tarball. Search ranking, indexing timing, and whether an AI retrieves the guide were not measured or guaranteed.
